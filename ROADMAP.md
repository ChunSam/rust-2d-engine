# rust-2d-engine 상용화 로드맵

> 작성일: 2026-05-25 | 기준 버전: v0.44.0 (Phase 43 완료)  
> 목표: **v1.0.0 공식 릴리즈** — 인디 게임 상용 배포 가능 수준

---

## 원칙

- **세부 계획은 각 Phase 직전에 수립** — 이 문서는 목표·범위·우선순위만 기록
- **병렬 처리 우선** — 파일 충돌 없는 작업은 항상 동시 진행
- **완료 기준 명시** — 각 Phase는 정량적 기준을 충족해야 완료로 간주
- **하위 호환 유지** — v1.0.0 이전까지는 파괴적 변경 허용, 이후 semver 엄수

---

## 마일스톤 개요

```
v0.44  ████████████████████████ 현재
         │
v0.50  ──┤ Milestone 1 — 게임 제작 가능
         │  필수 렌더/물리/입력 기능 완비
         │
v0.56  ──┤ Milestone 2 — 상용 배포 가능
         │  품질·안정성·플랫폼 완성
         │
v1.0.0 ──┤ Milestone 3 — 공식 출시
            문서·API freeze·생태계
```

---

## Milestone 1 — 게임 제작 가능 (v0.45 ~ v0.50)

> 목표: 장르 제한 없이 완성된 인디 게임을 제작할 수 있는 수준

### Phase 44 — 물리 확장 + 오디오 이펙트

**우선순위**: 높음 | **병렬 처리**: 44b + 44c 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 44b | 물리 조인트 (`DistanceJoint`, `RevoluteJoint`, `PrismaticJoint`) | Rapier2D joint API 래핑, 단위 테스트 |
| 44c | 오디오 이펙트 (`LowPassFilter`, 피치 시프트, 볼륨 엔벨로프) | 런타임 적용, 파라미터 실시간 변경 |

**배경**: 플랫포머·퍼즐 장르는 조인트 없이 구현 불가. 오디오 이펙트는 게임 피드백 품질 직결.

---

### Phase 45 — 시스템 실행 순서 명시

**우선순위**: 매우 높음 (구조적 기반) | **단독 실행**

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 45a | `SystemLabel` + `before/after` 의존성 선언 | DAG 정렬, 순환 감지 에러 |
| 45b | `SystemSet` (그룹 단위 on/off) | 디버그 모드 시스템 일괄 비활성화 |

**배경**: 현재 `add_system` 호출 순서에 의존 — 씬 교체 후 순서 역전 버그 발생 가능. 엔진 성숙도의 핵심 지표.

---

### Phase 46 — 렌더 텍스처

**우선순위**: 높음 | **단독 실행**

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 46a | `RenderTarget` 리소스 — 오프스크린 렌더 | 미니맵 예제 동작 |
| 46b | 렌더 텍스처 → Sprite로 사용 | 분할 화면(split-screen) 예제 |

**배경**: 미니맵, 포탈, UI 카메라, 분할 화면 없으면 전략·액션 장르 제한.

---

### Phase 47 — 터치 입력 + 모바일 지원

**우선순위**: 높음 | **병렬 처리**: 47a + 47b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 47a | `TouchState` — 멀티터치, 스와이프, 핀치줌 | Android/iOS 시뮬레이터 동작 |
| 47b | 가상 조이스틱 UI 위젯 | 모바일 게임 데모 동작 |

**배경**: 인디 게임 수익의 상당 부분이 모바일. 터치 없으면 iOS/Android App Store 출시 불가.

---

### Phase 48 — 물리 레이어/마스크 + 트리거

**우선순위**: 중간 | **병렬 처리**: 48a + 48b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 48a | `CollisionGroups` (비트마스크) — 선택적 충돌 | 레이어별 충돌 무시 동작 |
| 48b | 센서(trigger zone) — 충돌 없는 영역 진입 감지 | `TriggerEvent` 발생 확인 |

**배경**: 현재 모든 물체가 서로 충돌 — 복잡한 게임에서 성능·로직 문제 발생.

---

### Phase 49 — 텍스트 완성

**우선순위**: 중간 | **병렬 처리**: 49a + 49b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 49a | 멀티라인 텍스트 + 자동 줄바꿈 + 정렬 | 다국어 단락 정상 렌더링 |
| 49b | 리치 텍스트 (`[color]...[/color]`, 볼드, 이탤릭) | 인게임 다이얼로그 박스 예제 |

**배경**: 현재 텍스트는 단일 폰트·크기·색상만 지원 — 다이얼로그·UI 퀄리티 병목.

---

## Milestone 2 — 상용 배포 가능 (v0.51 ~ v0.56)

> 목표: 실제 스토어(Steam/iOS/Android) 배포에 필요한 품질·안정성 달성

### Phase 50 — 로컬라이제이션 (i18n)

**우선순위**: 높음 | **단독 실행**

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 50a | `LocaleResource` — `.ftl` 또는 `.json` 번역 파일 로드 | 한/영/일 전환 동작 |
| 50b | `t!("key")` 매크로 or API + 폰트 per-locale | RTL 언어(아랍어) 기본 지원 |

**배경**: 글로벌 출시 전에 i18n 붙이면 UI 전면 재작성 필요. 초기 설계가 필수.

---

### Phase 51 — 에셋 비동기 로딩

**우선순위**: 높음 | **병렬 처리**: 51a + 51b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 51a | 비동기 씬 로드 — `LoadingScene` + 진행률 리소스 | 로딩 바 예제 동작 (프리징 없음) |
| 51b | WASM `fetch` API 연동 — 브라우저에서 에셋 비동기 다운로드 | WASM 빌드에서 외부 에셋 로드 |

**배경**: 현재 동기 로딩 → 대용량 씬에서 수 초 프리징. 스토어 리뷰에서 치명적.

---

### Phase 52 — 안정성 + 패닉 복구

**우선순위**: 매우 높음 | **병렬 처리**: 52a + 52b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 52a | `std::panic::catch_unwind` 래퍼 — 시스템 패닉 격리 | 시스템 크래시 시 엔진 생존 |
| 52b | 에러 로그 파일 기록 + 크래시 리포트 형식 | `crash.log` 자동 생성 |

**배경**: 에셋 없음, 잘못된 씬 파일 등 런타임 오류가 즉시 크래시 → 상용 불가.

---

### Phase 53 — 저장 데이터 보안

**우선순위**: 중간 | **병렬 처리**: 53a + 53b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 53a | 저장 파일 암호화 (AES-GCM or XChaCha20) | 평문 RON 대체, 복호화 로드 |
| 53b | 체크섬 검증 — 파일 변조 감지 | 변조 시 `SaveError::Corrupted` |

**배경**: 현재 RON 평문 → 치트엔진으로 즉시 조작 가능.

---

### Phase 54 — 에디터 완성

**우선순위**: 중간 | **병렬 처리**: 54a + 54b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 54a | 멀티 선택 + 그룹 이동 + Ctrl+C/V | 여러 엔티티 동시 편집 |
| 54b | 프리팹 시스템 개선 — 인스턴스 오버라이드, 프리팹 브레이크 | 레벨 디자인 워크플로 완성 |

---

## Milestone 3 — 공식 출시 (v0.57 ~ v1.0.0)

> 목표: 외부 개발자가 독립적으로 사용할 수 있는 수준의 문서·안정성·생태계

### Phase 55 — 고급 렌더링

**우선순위**: 낮음 | **병렬 처리**: 55a + 55b 가능

| 서브 | 기능 |
|------|------|
| 55a | GPU 파티클 (컴퓨트 셰이더, 현재 CPU 파티클 대체) |
| 55b | 색상 그레이딩 (LUT 기반 포스트 프로세싱) |

---

### Phase 56 — Rustdoc 완성

**우선순위**: 매우 높음 (외부 사용자 진입점) | **단독 실행**

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 56a | 전체 공개 API rustdoc 작성 | `cargo doc --no-deps` 경고 0개 |
| 56b | 모듈별 `#[doc = include_str!("...")]` 가이드 | 각 서브시스템 개요 + 예제 포함 |

---

### Phase 57 — 예제 + 샘플 게임

**우선순위**: 높음 | **병렬 처리**: 57a + 57b 가능

| 서브 | 기능 | 완료 기준 |
|------|------|-----------|
| 57a | `examples/` 10개 — 각 서브시스템별 최소 예제 | `cargo run --example <name>` 전부 동작 |
| 57b | 샘플 미니게임 1개 (플랫포머 or 탑다운 RPG) | 실제 게임 완성도 수준 |

---

### Phase 58 — API Freeze + v1.0.0 출시

**우선순위**: 최고 | **단독 진행**

| 항목 | 내용 |
|------|------|
| API 감사 | 공개 API 전체 검토 — 명명 일관성, 불필요 노출 제거 |
| Semver 선언 | 이후 파괴적 변경은 v2.0.0 — CHANGELOG 형식 도입 |
| 성능 벤치마크 | 엔티티 10,000개 60fps 기준선 문서화 |
| 보안 감사 | unsafe 블록 전수 검토, 메모리 안전성 확인 |
| 공식 릴리즈 | GitHub Release + crates.io 게시 |

---

## 우선순위 매트릭스

```
                 임팩트 높음          임팩트 낮음
구현 쉬움    시스템 오더링 (45)    저장 암호화 (53)
             물리 레이어 (48)      색상 그레이딩 (55b)
             
구현 어려움  렌더 텍스처 (46)      GPU 파티클 (55a)
             터치 입력 (47)        2D 스켈레탈 애니메이션
             로컬라이제이션 (50)
```

**즉시 시작 권장**: Phase 44 → 45 → 46 (이 세 개가 이후 모든 기능의 기반)

---

## 버전별 체크리스트 (요약)

| 버전 | 완료 조건 |
|------|-----------|
| v0.46 | Phase 44 + 45 완료. 시스템 순서 보장, 조인트 동작 |
| v0.48 | Phase 46 + 47 완료. 렌더 텍스처, 터치 입력 |
| v0.50 | Phase 48 + 49 완료. Milestone 1 달성 |
| v0.52 | Phase 50 + 51 완료. 로컬라이제이션, 비동기 로딩 |
| v0.54 | Phase 52 + 53 완료. 안정성, 저장 보안 |
| v0.56 | Phase 54 + 55 완료. Milestone 2 달성 |
| v0.58 | Phase 56 + 57 완료. 문서, 예제 |
| **v1.0.0** | Phase 58 완료. API freeze, crates.io 게시 |

---

*각 Phase의 세부 구현 계획은 해당 Phase 시작 직전에 별도 수립한다.*  
*이 문서는 방향과 우선순위만 기록하며, 진행에 따라 갱신된다.*
